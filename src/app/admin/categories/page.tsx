"use client";
import {useEffect,useState} from "react";import {Plus} from "lucide-react";import {Button} from "@/components/ui/button";import {Card,CardContent,CardDescription,CardHeader,CardTitle} from "@/components/ui/card";import {Input} from "@/components/ui/input";import {Badge} from "@/components/ui/badge";
type Category={id:string;nameEn:string;nameKm:string;status:"active"|"inactive";itemCount:number};
export default function CategoriesPage(){const [items,setItems]=useState<Category[]>([]);const [show,setShow]=useState(false);const [error,setError]=useState("");async function load(){const r=await fetch('/api/admin/categories');setItems(await r.json() as Category[])}// Initial remote load intentionally writes the client cache after mount.
// eslint-disable-next-line react-hooks/set-state-in-effect
useEffect(()=>{void load()},[]);async function save(form:FormData){setError("");const r=await fetch('/api/admin/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});if(!r.ok){setError((await r.json() as {error?:string}).error??'Could not create category.');return}setShow(false);await load()}return <main className="flex flex-1 flex-col gap-6 p-4 md:p-6 lg:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h1 className="text-2xl font-semibold tracking-tight md:text-3xl">Categories</h1><p className="mt-1 text-sm text-muted-foreground">Organize customer menu items in Khmer and English.</p></div><Button onClick={()=>setShow(true)} className="min-h-11"><Plus/>Create category</Button></div>{show&&<Card className="max-w-2xl"><CardHeader><CardTitle>New category</CardTitle><CardDescription>A category can be used as soon as it is created.</CardDescription></CardHeader><CardContent><form action={save} className="grid gap-4 sm:grid-cols-2"><div className="grid gap-2"><label htmlFor="nameEn" className="text-sm font-medium">English name</label><Input required id="nameEn" name="nameEn"/></div><div className="grid gap-2"><label htmlFor="nameKm" className="text-sm font-medium">Khmer name</label><Input required id="nameKm" name="nameKm" lang="km"/></div>{error&&<p role="alert" className="text-sm text-destructive sm:col-span-2">{error}</p>}<div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="outline" onClick={()=>setShow(false)}>Cancel</Button><Button>Create category</Button></div></form></CardContent></Card>}<Card><CardContent className="p-0"><div className="divide-y">{items.map(category=><div key={category.id} className="flex items-center justify-between gap-4 p-4"><div><p className="font-medium">{category.nameEn}</p><p lang="km" className="mt-1 text-sm text-muted-foreground">{category.nameKm}</p></div><div className="flex items-center gap-4"><span className="text-sm text-muted-foreground">{category.itemCount} items</span><Badge variant={category.status==='active'?'default':'secondary'}>{category.status}</Badge></div></div>)}{!items.length&&<p className="p-8 text-center text-muted-foreground">No categories yet.</p>}</div></CardContent></Card></main>}
